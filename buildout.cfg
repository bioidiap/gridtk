[buildout]
parts = scripts
develop = .

; required packages: our package
; optional package: ipdb (for debugging)
eggs =  gridtk
        ipdb
        sphinx-pypi-upload

[scripts]
recipe = xbob.buildout:scripts

